# Use an official Nginx image as the base
FROM nginx:alpine

# Copy the static content (html, css, js) into the default Nginx web directory
COPY . /usr/share/nginx/html

# Expose port 80 (default for Nginx)
EXPOSE 80